# Bike-Speed-Percentile
Takes user input to calculate their percentile compared to a dataset. Statistic measured is highest m/s on a bicycle.
(Note: You may have to run a "install pandas" command in order for this program to work. I used "!mamba".
